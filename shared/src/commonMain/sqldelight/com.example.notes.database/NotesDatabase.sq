CREATE TABLE saved_notes (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    content TEXT NOT NULL
);

insertNote:
INSERT OR REPLACE INTO saved_notes(id,title,content)
VALUES (?,?,?);

getAllSavedNotes:
SELECT *
FROM saved_notes;

deleteNote:
DELETE FROM saved_notes WHERE id == :id;


